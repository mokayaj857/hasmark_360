# Backend port (default: 4000)
PORT=4000

# Ethereum JSON-RPC URL
# Local Hardhat:   http://127.0.0.1:8545
# Sepolia testnet: https://rpc.sepolia.org
# Infura Sepolia:  https://sepolia.infura.io/v3/YOUR_INFURA_KEY
RPC_URL=http://127.0.0.1:8545

# Deployed Hashmark contract address (auto-filled by scripts/deploy.js)
CONTRACT_ADDRESS=

# Server-side wallet private key — OPTIONAL
# If left empty, authentication is done client-side via MetaMask (recommended).
# NEVER commit a real private key.
PRIVATE_KEY=

# For contract deployment to Sepolia testnet
SEPOLIA_RPC_URL=
DEPLOY_PRIVATE_KEY=

# Frontend origin for CORS (comma-separated for multiple)
CORS_ORIGIN=http://localhost:5173

# Frontend URL used when generating QR code verify links
FRONTEND_URL=http://localhost:5173

# World Bank Data360 API
# Default base URL targets the official Data360 API.
DATA360_BASE_URL=https://data360api.worldbank.org
# Optional API key + query param name if your Data360 access requires one
DATA360_API_KEY=
DATA360_API_KEY_PARAM=api_key
