# ──────────────────────────────────────────────────────────────────────────────
# Hashmark Protocol — Vercel Environment Variables
#
# Set ALL of these in your Vercel dashboard:
#   Project → Settings → Environment Variables
#
# IMPORTANT: VITE_ variables are embedded at build time.
#            Non-VITE_ variables are used by the serverless backend at runtime.
# ──────────────────────────────────────────────────────────────────────────────

# ── BACKEND (runtime, set in Vercel dashboard) ────────────────────────────────

# Deployed contract address on Sepolia (run: cd back && npm run deploy:sepolia)
CONTRACT_ADDRESS=

# Sepolia RPC — get a free key from https://dashboard.alchemy.com
# Example: https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY
RPC_URL=https://rpc.sepolia.org

# Your production URL (replace with actual Vercel URL after first deploy)
FRONTEND_URL=https://hashmark-base.vercel.app

CORS_ORIGIN=https://hashmark-base.vercel.app
NODE_ENV=production

# ── FRONTEND (build-time, MUST be set in Vercel dashboard with VITE_ prefix) ─

# Same contract address as above
VITE_CONTRACT_ADDRESS=

# Sepolia chain ID
VITE_CHAIN_ID=11155111

# Public Sepolia RPC for MetaMask network switching
VITE_RPC_URL=https://rpc.sepolia.org
