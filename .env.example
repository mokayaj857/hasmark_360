# Frontend env vars — copy to .env and fill in values after deploying the contract
# Run: cd back && npx hardhat node   (starts local node)
#      cd back && npx hardhat run scripts/deploy.js --network localhost
# The deploy script auto-fills VITE_CONTRACT_ADDRESS below.

# Deployed Hashmark contract address (auto-filled by deploy.js)
VITE_CONTRACT_ADDRESS=

# Chain ID of your target network
# 1337  = Hardhat local
# 11155111 = Sepolia testnet
VITE_CHAIN_ID=1337

# JSON-RPC URL used for read-only calls when MetaMask is not connected
VITE_RPC_URL=http://127.0.0.1:8545
